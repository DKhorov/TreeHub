
<div align="center">

# 🌿 TreeHub API 

![Python](https://img.shields.io/badge/Python-3.10+-blue?logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-Framework-brightgreen?logo=fastapi)
![MongoDB](https://img.shields.io/badge/MongoDB-Database-green?logo=mongodb)
![License](https://img.shields.io/badge/License-MIT-lightgrey)
![Version](https://img.shields.io/badge/Version-3.0.0-orange)

> 💡 Универсальный API-сервер с веб-интерфейсом, логированием, shell-консолью и встроенным управлением системой.

</div>

---

## 📖 Описание

**TreeHub API** — это модульная система на Python, которая объединяет:

- 🔹 FastAPI REST сервер с JWT-аутентификацией  
- 🔹 MongoDB базу данных  
- 🔹 Встроенный Shell (`shelldll.py`) для управления сервером  
- 🔹 Системные утилиты, логи, web-сервер и панель администратора  
- 🔹 Минималистичный конфиг через `dataApi.txt`

Простой запуск — максимум функционала 💪  

---

## 🏗️ Архитектура проекта

```

TreeHub/
├── server.py          # точка входа
├── serverdll.py       # основной FastAPI сервер
├── shelldll.py        # консоль управления (TreeHub Shell)
├── databasedll.py     # работа с MongoDB
├── modelsdll.py       # модели данных (Pydantic)
├── logapidll.py       # логирование
├── cashdll.py         # защита от DDoS / rate-limit
├── system_info.py     # информация о системе
├── dataApi.txt        # конфигурация БД
├── log/               # логи системы
│   ├── data.log
│   ├── api.log
│   ├── web.log
├── site/              # веб-контент (index.html и т.д.)
└── commands.md        # список доступных команд shell

````

---

## ⚙️ Технологии

| Компонент        | Технология / Библиотека             |
|------------------|-------------------------------------|
| 🧠 API Framework | [FastAPI](https://fastapi.tiangolo.com/) |
| 💾 База данных   | [MongoDB](https://www.mongodb.com/) + pymongo |
| 🔐 Аутентификация | JWT (через `python-jose`)          |
| 🧩 CLI / Shell   | Встроенная оболочка TreeHub Shell   |
| 🧰 Логи          | Собственный логгер (`logapidll.py`) |
| 🌍 Веб сервер    | Python `http.server` встроенный     |
| 💻 Системная инфо | psutil, platform, prettytable       |

---
## Для какиз версий AtomGlide доступен API 
Atomglide 0.1 Beta 1
AtomGlide 0.1 Beta 2
AtomGlide 0.1 Beta 3
---
## 🚀 Установка и запуск

### 🧩 Требования

| Компонент | Минимум |
|------------|----------|
| Python     | `3.14` или выше |
| RAM        | `512 MB` |
| CPU        | 1 ядро |
| MongoDB    | `v5.0+` |
| OS         | Windows / Linux / macOS |

---

### 🔧 Установка

```bash
# 1️⃣ Клонируем репозиторий
git clone https://github.com/dkhorov/TreeHub.git
cd treehub

# 2️⃣ Устанавливаем зависимости
pip install -r requirements.txt
````

**requirements.txt:**

```txt
fastapi
uvicorn
pymongo
python-jose
pydantic
colorama
prettytable
art
psutil
```

---

### ▶️ Запуск сервера

#### Вариант 1: Через shell

```bash
python server.py
```

Затем ввести:

```
$set
```

> Сервер автоматически поднимет FastAPI на `http://0.0.0.0:8000/`.

#### Вариант 2: Через uvicorn напрямую

```bash
uvicorn serverdll:server --host 0.0.0.0 --port 8000
```

---

## 🧠 Примеры API-запросов

### 🔹 Регистрация пользователя

```bash
POST /register
Content-Type: application/json

{
  "username": "Dmitry",
  "usernick": "dmitryx",
  "password": "123456",
  "love": "Python"
}
```

### 🔹 Авторизация

```bash
POST /login
{
  "usernick": "dmitryx",
  "password": "123456"
}
```

### 🔹 Доступ к профилю

```bash
GET /profile
Authorization: Bearer <access_token>
```

---

## 💡 Примеры команд TreeHub Shell

| Команда           | Описание                                        |
| ----------------- | ----------------------------------------------- |
| `$set`            | Запуск API сервера                              |
| `$swagger on/off` | Включить / выключить Swagger UI                 |
| `$web start`      | Запустить веб-сервер (папка `site/`)            |
| `$web open`       | Открыть сайт в браузере                         |
| `$get users`      | Показать пользователей (username, usernick)     |
| `$db stats`       | Статистика MongoDB                              |
| `$log tail`       | Последние строки лога                           |
| `$reset logs`     | Очистить все логи                               |
| `$status`         | Проверка состояния сервисов                     |
| `$get commands`   | Создание файла `commands.md` со всеми командами |

---

## 🌐 Web Server

Встроенный HTTP-сервер позволяет быстро запустить локальный сайт:

```bash
$web start
```

➡ Откроется на: `http://127.0.0.1:8080/`

Папка для сайта:

```
site/
└── index.html
```

---

## 🔐 Безопасность

* Rate Limit: до 5 запросов в минуту с одного IP
* JWT-токены (автоматически истекают через 60 минут)
* Логирование всех действий в `log/data.log`
* Возможность отключить Swagger (через `$swagger off`)
* IP и пароли не сохраняются в открытых логах

---

## 📊 Пример логов

```text
[2025-11-04 12:00:01] ✅ Новый пользователь: Dmitry (dmitryx), IP: 192.168.1.10
[2025-11-04 12:01:22] ⚠️ Неверный пароль: test (IP: 192.168.1.15)
[2025-11-04 12:02:00] [shell] Запуск API сервера
[2025-11-04 12:02:10] [web] Start HTTP server on 0.0.0.0:8080
```

---

## 🧰 Настройки MongoDB

Файл `dataApi.txt`:

```ini
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/db
DB_NAME=treehub
COLLECTION=users
```

---

## ⚙️ Swagger

После включения:

```
$swagger on
```

Swagger UI доступен по адресу:
👉 `http://127.0.0.1:8000/docs`

---

## 💾 Бэкап и резервирование

* Логи API можно сохранить:

  ```
  $get api-log-save
  ```
* Создать резервную копию `api.log`:

  ```
  log/api_backup_YYYYMMDD_HHMMSS.log
  ```

---


## 💬 Контакты

**Автор:** [@Dmitry Khorov](https://github.com/dkhorov)
**Проект:** [TreeHub API](https://github.com/dkhorov/TreeHub)

---

<div align="center">

Проект OpenSource ! Без юр силы 
Поддержка MIT , DK Studio OLC 

</div>
  
